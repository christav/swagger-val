<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv='X-UA-Compatible' content='IE=edge' />
        <meta name='viewport' content='width=device-width, initial-scale=1' />
        <link rel='stylesheet' href='/public/css/bootstrap.min.css' />
        <link rel='stylesheet' href='/public/css/style.css' />
        <script src='/public/js/jquery.min.js'></script>
        {{{block "head"}}}
    </head>

    <body>
        <div class='navbar navbar-inverse navbar-fixed-top' role='navigation'>
            <div class='container'>
                <div class='navbar-header'>
                    <button class='navbar-toggle' type='button' data-toggle='collapse' data-target='.navbar-collapse'>
                        <span class='sr-only'>Toggle navigation</span>
                        <span class='icon-bar'/>
                        <span class='icon-bar'/>
                        <span class='icon-bar'/>
                    </button>
                    <a class='navbar-brand navbar-text' href='/'>Azure Auth UI</a>
                </div>
                <div class='collapse navbar-collapse'>
                    <ul class='nav navbar-nav'>
                        <li>
                            <a class='navbar-text' href='/'>Home</a>
                        </li>
                        <li>
                        {{#if authorized}}
                            <a class='navbar-text navbar-right' href='/auth/logout'>Log out</a>
                        {{else}}
                            <a class='navbar-text navbar-right' href='/auth/login'>Log in</a>
                        {{/if}}
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <div class='container content-block'>
          {{block "body"}}
        </div>

        <script src='/public/js/bootstrap.min.js'></script>
    </body>
</html>